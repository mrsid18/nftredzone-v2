import React from "react";
import GenericSection from "./GenericSection";

const MintInfo = () => {
  return (
    <div className="container pb-32 mb-20 reveal-from-bottom" data-reveal-delay="200">
      <p className="text-color-high ta-c fw-600">
        Minted cards reveal once per week on Thursday, before Thursday night
        football and the start of that week. All revealed cards are eligible for
        the current week’s contests. Similar to lineup lock in fantasy football.
      </p>
    </div>
  );
};

export default MintInfo;
